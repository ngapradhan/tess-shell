pipeline {
    agent any

    stages {
        stage('Run Shell Script') {
            steps {
                script {
                    // Run the shell script directly from the repository
                    steps.echo"Hello world"
                }
            }
        }
    }
}
